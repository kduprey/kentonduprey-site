services:
  db:
    image: postgres:latest
    restart: unless-stopped
    environment:
      POSTGRES_USER: kenton
      POSTGRES_PASSWORD: duprey
      POSTGRES_DB: kenton_duprey
    ports:
      - "5438:5432"
    volumes:
      - kduprey:/var/lib/postgresql

volumes:
  kduprey:
